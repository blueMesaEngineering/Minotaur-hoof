<% provide(:title, "New model") %>

<h1>New Model</h1>
<center>

	<%= form_with scope: :url_data_model, url: url_data_models_path, local: true do |form| %>
		<p>
			<%= form.label :address %>
			<%= form.text_field :address %>
		</p>

		<p>
			<%= form.submit %>
		</p>
	<% end %>
</center>